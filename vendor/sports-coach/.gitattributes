# Preserve the imported bytes (including CRLF) for source-manifest.json verification.
# Keep whitespace checks active while recognizing CRLF as a valid line ending.
0*/** -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
