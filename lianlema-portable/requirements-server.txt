# Install torch CPU first from https://download.pytorch.org/whl/cpu (Python 3.11).
# Isolated venv only; do not install into another project's environment.
Flask==3.1.3
gunicorn==23.0.0
numpy==1.26.4
opencv-contrib-python==4.10.0.84
opencv-python==4.10.0.84
onnxruntime==1.20.1
rtmlib==0.0.15
Pillow==11.1.0
