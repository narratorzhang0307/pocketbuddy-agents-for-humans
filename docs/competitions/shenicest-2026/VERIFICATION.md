# Pocket Buddy · 本次源码交付与验证

## 补充：Web Demo 上线验证（2026-08-28）

项目体验地址：[https://pocket-buddy.throughtheglass.art/](https://pocket-buddy.throughtheglass.art/)。README 和项目文档最前面也提供同一个入口。

- 应用构建对应提交 `d51a73b830795a22e7cfc54d574081415ec402f6`；运动代理的同源校验另见配置提交 `27171e83713912b056d6aac1286bb5d5d97cb116`。三个 Web 应用从源码重新构建，未使用旧 Web 分包或 iOS 安装包。
- 构建源码指纹：`9c249ee2a5828233883a2ca162e18b1621df0d520a1b88ec5e989d5357c49f0b`；练了吗源码指纹：`7c4fa8b5eb7db3f49e749dc8254d66de051488d2e1c2b79437d5001832726742`。实际线上 `/release.json` 已核对一致。
- 回归验证：类型检查通过；**2,781 项测试通过、16 项跳过**（225 个文件通过、3 个文件跳过）；识鸟、技能画布和 Frost Skills 校验通过。首次测试因本机临时磁盘空间不足中断，改用外置盘临时目录后完整重跑通过，未删除失败测试。
- 发布包共 **1,264 个文件 / 427,133,883 字节**，已扫描并确认没有私有模型凭据、私钥或高德安全码。服务器逐文件 SHA-256 与新构建清单一致。传输中仅对与新产物完全同字节的静态媒体利用服务器已有副本省去上传；没有采用旧 JS、CSS、HTML 或旧发布清单。
- 新域名拥有独立目录、Node 服务、端口、数据目录及 Nginx 配置。原站的配置摘要和 `current` 目标保持不变，原站健康接口仍正常。运动和 SAM 使用已有回环模型服务，未重启它们，也未复制旧用户数据。
- HTTPS 证书匹配新域名，有效期至 **2026-11-26**；已有证书续期定时器及新域名续期后的 Nginx reload 钩子已核对。HTTP 跳转到 HTTPS。
- 外网已验证：首页、实际发布标记、两个子应用页面及嵌入策略、私有/缺失文件返回 404、运动和 SAM 真实依赖健康、陌生来源与未明确同意的分析请求被拒绝。
- Qwen 实际返回“连接成功”；以临时合成的纯灰图测试真实 RTMO 推理，结果为 `no_person`、0 次动作，不伪造识别结果；测试会话已停止。没有使用用户相机、私人照片或健康数据。
- 浏览器已核对高德地图、行动地图控件、Photos 示例及其非实时识别提示、Agents 核心技能列表和主导航实际显示，未发现浏览器错误日志。初次资源加载在本次网络环境下偏慢，仍需优化首屏分包与静态资源传输。

边界：SAM 依赖健康与照片同意校验通过，不等于已经完成真人餐食精度验收；不包含实体徽章、iOS 安装、真实运动动作或临床验证。端侧模型未在网页服务器启用，受访问码保护的专用能力不会被擅自解除保护。部署细节见 [参赛 Demo 部署说明](../../../deploy/pocketbuddy/competition-demo/README.md)。

以下保留的是初始源码交付记录（`b13efb9`）。“未部署”“未调用真实云服务”等表述只描述初始交付阶段，当前 Web 上线情况以上述补充记录为准。

## 交付边界

- 目标仓库：`narratorzhang0307/Pocket-Buddy`；默认源码分支：`main`；Topic：`shenicest-fission`。
- 源码来自当前正式工作区的实际文件，不使用旧 App、历史构建目录或旧仓库的冻结源码替代。
- 最终源码采集时间：**2026-08-28 20:33:07（北京时间）**。复制完成后再次核对源文件，采集过程中的内容一致。
- 在外置 SSD 的独立目录安装依赖、执行检查并创建新的 Git 历史；不向旧 `pocketbuddy` 或原工作区的其他远端推送，不改原工作区分支和暂存区。
- 新仓库保持私有。本次不更改可见性、不添加协作者、不代替赛事报名或最终提交。

保留主应用、两个训练子应用、原生桥接、徽章固件、服务端源码、构建脚本、锁文件、现行资源及有效产品文档。原项目的历史稳定版本说明保留为背景资料，不作为本次重新装机或线上部署的证明。

## 本次不上传的内容

- 真实 `.env`、云服务密钥、运行时凭据、Apple 签名与描述文件。
- 私人照片、原始录音、健康导出、设备备份与私人验收截图/日志。
- `node_modules`、`dist`、`dist-ios`、生成的子应用发布目录、IPA / APK 及其他构建产物。
- 按现有忽略规则独立管理的大型模型权重、训练数据和工具链。当前应用所需且已纳入源码的运行时资源不因此被任意删除。
- 历史 `docs/evidence`、`docs/technical-evidence`、`docs/design`、`docs/development/evidence` 以及旧 OSS 发布清单；部分历史文档指向这些本机资料的链接不会随快照发布。
- 已在现行发布策略中退役的 `public/mediapipe` 和 `public/signbridge` 独立运行时；Her Motion 自己使用的 MediaPipe 资源保留。
- GitHub Actions 工作流：沿用原工作区的排除设置，不额外启用自动部署或自动发布。

执行了文件名、常见令牌/私钥/凭据格式与文本配置扫描，并检查待提交文件列表。扫描命中项为测试夹具中的合成占位值；没有把它们误当成真实服务凭据。此检查不等同于正式安全审计，第三方组件和模型许可仍需按其来源遵守。

## 实际检查结果

环境：macOS，Node.js **v24.12.0**，npm **11.6.2**。主应用、练了吗与 Her Motion 均从各自锁文件执行了新的 `npm ci`，未上传本机依赖目录。

最终检查于 **2026-08-28 20:34:38（北京时间）**完成：

| 检查 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm test -- --reporter=dot --silent` | **2,773 通过，16 跳过，0 失败**；225 个测试文件通过，3 个按配置跳过 |
| `npm run ios:test` | **13 通过，0 失败** |
| `npm run bird:check` | 通过；`bird-skill-pet-birds-v3` |
| `node deploy/pocketbuddy/build-coach-web.mjs` | 练了吗 Web 子应用构建通过 |
| `node node_modules/vite/bin/vite.js build --config vendor/her-motion/vite.config.ts` | Her Motion Web 子应用构建通过 |
| `npm run build` | 主应用构建通过；包含 Frost Skills 的构建后检查 |
| `node scripts/ios/verify-skill-canvas.mjs dist` | 通过；`editorial-eaffa7f-v1` |
| `node scripts/hardware/check-bird-release.mjs --web-dir dist` | 通过；实际 Web 鸟类资源与清单一致 |

上述连续检查前后的交付文件内容指纹一致，没有边检查边更换源码。指纹对应最终验证时的 3,046 个交付文件（含参赛项目文档，不含本记录自身），按相对路径排序，依次将 `路径 + NUL + 文件原始内容` 输入 SHA-256：

```text
47e863616966d38a414164e6069d5128f56bb3ce3670efb0b277646f522e1f44
```

整理过程曾检测到视频测试与实现更新不同步，产生 3 项失败。之后重新从实际工作区采集完整、相互对应的文件，再完成了上表中的全量验证；没有通过删除测试或修改断言掩盖问题。

## 警告与未验证事项

- Vite 仍提示部分压缩后的 JS 分包大于 900 kB，后续可继续优化首屏加载与分包；此次不为上传而重构产品。
- 依赖安装出现部分上游包弃用提示，未擅自升级锁文件。测试通过不是依赖漏洞审计结果。
- 既有源码和第三方文件中存在行尾空白 / CRLF 格式提示；保留原文件，未做无关的全仓格式重写。新写的参赛文档单独检查。
- 跳过的测试没有被算作通过。此次没有访问真实云端推理来证明服务在线，也没有调用付费模型接口进行真人场景验收。
- 未进行 iOS 归档、签名、安装、刷机、TestFlight 上传、锁屏或 BLE 真机验收。Web 构建与脚本测试不能替代这些验证。
- 未部署网站或修改线上服务。比赛演示仍须单独确认地图配置、独立模型服务、摄像头/定位/蓝牙权限及评审访问方式。

## 文档与赛前基础

[参赛项目文档](./PROJECT.md)已包含项目背景、目标用户、玩法、技术栈、创新点、开发过程及后续计划；团队分工保持空白。

本次初始提交是源码交付节点，不是全部功能的原创时间证明。团队仍需核实赛道/命题，补充赛期增量、素材与开源来源，并在赛事要求时提交二次开发申报。
