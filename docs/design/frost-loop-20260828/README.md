# Frost 圆屏常驻动效 · 已烧录

> 后续更新：眯眼停留已按用户反馈延长到 1.6 秒，并安装 0.2.18。此页保留 0.2.17 历史证据，最新记录见 `../frost-blink-20260828/README.md`。

用户要求小狗持续动嘴和眨眼，因此在声音驱动版基础上启用离线待机循环。录音、头像上传、显示其他技能头像或进入其他专用显示模式时暂停；退出后恢复。没有新增录音权限，也没有修改手机应用。

## 固件与素材

- 已安装：`0.2.17-ojbadge-frost-loop`。
- 固件：2,361,856 字节，SHA-256 `2836b5c72a570fb1d3308d1d76b9976f5bb73e801050b4537aeb670201f3f564`。
- 3 MiB 应用分区剩余 783,872 字节。
- 复用原图及已生成的三张表情 JPEG，新增素材仍为 45,868 字节，不增加整屏缓冲。其他头像继续使用原 OSS 方案。
- 循环预览：`public/assets/frost-talk/20260828/index.html`，GIF/WebP 是示意，不是实机录屏。

## 烧录安全与验证

直接核实 USB 身份、Flash 容量和安全状态，读取设备确认原应用为 `0.2.15-ojbadge-birds`。烧录前完整备份 3 MiB 当前应用槽和前 64 KiB 启动/元数据；写入前再次校验备份与设备一致。

仅写 `0x10000` 应用分区，10 块物理读回与候选镜像逐字节一致。前 64 KiB 再次读回完全一致，包括 bootloader、分区表、NVS 与 OTA 数据。没有擦整片 Flash，也没有操作手机。

独立冻结源码构建通过；C++ 主机测试与 4 文件共 93 项头像测试通过。原 Frost PNG、原头像数组/标签和 OSS catalog 与修改前 SHA-256 一致。

## 实机运行证据

串口记录到新版本号、首个 LCD DMA 传输完成；30 秒观察中帧计数由 1 增长到 159，四种表情的位图标志为 `0x0f`，没有捕获解码错误、看门狗或崩溃。详见 `live-loop.log` 和 `verification.json`。

当前 JPEG 解码与显示的帧切换实测约 6.2 次/秒，记录到的单帧最长解码约 127.5 ms。持续动画会增加耗电，实际功耗和肉眼流畅度仍需现场确认；**没有新实拍照片，未宣称已进行肉眼验收**。

观察结束后串口已关闭释放，小狗保持运行。回滚备份和烧录脚本保存在本机私有目录 `/Users/zhangcheng/.local/share/pocketbuddy-esp/frost-loop-20260828/`，独立构建位于 `/Volumes/PocketBuddy-iOS-Dev/Artifacts/frost-loop-20260828/`。不要将私有 Flash 备份提交或上传。
