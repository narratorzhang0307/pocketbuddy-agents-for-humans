# 当前正式工程的整包发布

## 原因与规则

此前 `testflight-presence` 使用冻结源码加局部覆盖，可能遗漏其他任务已在正式根目录完成的修改。该方式已退役；旧归档、符号和历史发布记录仅用于追溯，不是后续发布的源码基线。

正式源码目录：`/Users/zhangcheng/Desktop/谷歌大赛`。当前尚未提交的源码也属于本次构建输入，不从 Git 历史重置或恢复。

```sh
node scripts/ios/archive.mjs --build-number <已在苹果后台核对未使用的构建号>
```

这个入口强制执行 TypeScript 检查、构建来源回归测试、完整 `ios:prepare`、新目录 Release 归档和实际 App 验证。不接受 `--prepared`、历史源码路径或旧 App；不会自动上传、安装或删除用户数据。

## 验证范围

- 主应用全部源码、public 资源、原生桥、桌面组件、Xcode 配置及依赖锁文件均参与指纹校验。
- 训练与 Her Motion 子应用先由当前源码重新导出，再与主应用一起完整同步；不沿用旧发布副本中的 public。
- 技能画布必须是 `editorial-eaffa7f-v1`；失败时不回退旧波浪卡片。
- Frost Skills 必须是默认折叠新版；识鸟必须是 `bird-skill-pet-birds-v3`。
- 归档后对实际 App 的全部 Web 文件逐文件比对，额外旧文件、缺失资源、修改过的文件都会失败。
- 主 App 与 Widget 的构建号、签名及 18 张 Widget 头像单独核验。
- `.ios-build/latest-archive.json` 指向通过验证的归档，并明确记录是否已经上传；归档完成不代表 TestFlight 可下载。

构建时独占 `.ios-build/install.lock`。其他任务若继续修改源码，验证会拒绝过期产物，必须重跑全量构建。不要删除锁绕开仍在运行的任务。

发布记录将另列实际归档路径、源码指纹、资源指纹和 TestFlight 状态；不得仅凭版本号宣称手机已收到新版。

## 本轮追加：可移除的 Photos 示例

- 首次默认显示考伯碗识别示例与四餐记录示例，复用 `public/assets/food-demo` 中原有 FoodSense 图片。
- “移除示例”只保存本机显示偏好，重新打开仍隐藏；“恢复餐食示例预览”可恢复。不会清除用户照片或餐食记录，也不承诺删除签名 App 内置图片文件。
- 示例是独立展示组件，不接收/产生 MealCandidate，不触发 Qwen/SAM，不调用健康记忆写入；示意框和营养数值明确标为演示。
- 选择真实照片或手动记录时退出示例展示，继续使用现有真实识别和确认流程。
- `check.mjs` 检查实际 Photos 分包中的移除/恢复入口及四张示例图片，防止下次构建遗漏。
- 原候选 `2026082820` 的导出上传流程已取消，未分发作为最终更新。加入本次修改后从完整源码重建 `2026082821`，不是修改旧包。

## 本轮追加：医院 Agent 自动使用 Qwen

- 已删除页面的后端连接、重新检测连接和部署说明；不再依赖额外的 `HOSPITAL_AGENT_BASE_URL`。
- 复用现有 `requestQwenText` → `/api/frost-llm`，任务为 `subagent:hospital-agent`。由服务端 `QWEN_MODEL_SUBAGENT` 选择模型，客户端没有 API 密钥或可输入的服务地址。
- 2026-08-28 对正式 HTTPS 服务发送不含个人资料的连通测试，HTTP 200，实际返回模型 `qwen3.8-max`；不据此声称临床验证或完整多角色流程已运行。
- 打开页面不发模型请求。用户点“同意并发送给 Qwen”后，只上传当前问题和所选科室的公开目录；不读取健康账本、照片、历史对话，也不自动保存到健康记忆。
- 保留私有 `hospital-consultation` 接口原有认证，不为了页面接入移除其访问保护。
- 医院目录纳入源码指纹，实际 iOS 分包强制检查新问答入口并拒绝旧配置面板。
- `2026082821` 已归档但未上传；加入医院页后，最终候选从完整源码重新构建为 `2026082822`。

## 2026082822 发布记录（图标回退，已发现并修复）

`0.1.0 (2026082822)` 已于 2026-08-28 上传并通过 Apple 处理，加入现有 `Owner Internal Test`，后台已确认“正在测试”。构建 ID 为 `aab499bb-e75d-46a7-97a3-673bad24689d`。

归档、分发 IPA 和复核记录位于 `/Volumes/PocketBuddy-iOS-Dev/Current/Releases/2026082822-SfThQb`，`.ios-build/verification-2026082822.md` 保存具体检查结果。此历史版本已由下面的 23 号修复版取代；`latest-archive.json` 跟随最新通过验证的发布。

## 2026082823 图标修复整包

用户发现 22 号包显示旧地球图标。原因与修复见 [图标回退记录](APP-ICON-REGRESSION-2026-08-28.md)。
本次将已确认的 Frost 小狗眨眼图标归回正式源码，并增加图片指纹及实际编译图标检查。
没有复制旧源码或旧可执行包；仍完整执行主应用、两个子应用和原生工程构建。

- 2026-08-28 14:19:41 CST 上传成功；14:27 CST 已确认 Apple 后台“正在测试”，加入现有 `Owner Internal Test`（1 位测试员），可在 TestFlight 更新。
- Apple 构建 ID：`fb35c260-92a4-4625-acbe-d1e399045840`。测试说明在添加测试组前保存，包含桌面组件、灵动岛与入场预览的使用步骤。
- 主 App 与桌面组件构建号均为 `2026082823`，18 个组件头像、共享 App Group 和分发签名通过检查。
- 实际归档及解包后的分发 IPA 均通过来源、画布、Frost Skills、识鸟与小狗图标检查。
- 全量测试 2673 通过、16 跳过；TypeScript 通过；构建来源与图标回归 13 通过。
- 独立 GitHub 源码目录使用三个锁文件重新安装依赖，完整重建两个子应用和主应用成功；再次通过同样的测试。
- 源码 SHA256：`a4acb844bd629df898847fa2bc1f573d778f34b51afd8dc9d0004fb3788f1610`。
- Web SHA256：`99760b383270b88e1a04656308e3a1c9b31ae0f736daf245e7244277a3ecca39`。
- IPA SHA256：`6bea745498e6735f84e7a6968c916721de10e22476665228802661bc140af02c`。

产物位于 `/Volumes/PocketBuddy-iOS-Dev/Current/Releases/2026082823-lm4LC1`。
没有安装/卸载用户手机上的 App，也未清除记录。小组件和灵动岛保留，不把静态品牌图标宣称为持续动画。

## 2026082824 识鸟专项整包复核

本轮逐文件比对确认：直接打开识鸟 Skill、自动准备 B 板触屏录音、原生状态驱动页面导航和十二鸟回图代码已经包含在 23 号包及 `cd82d29f` 中。
按用户要求从正式工作区再次全量构建，不从此前归档复制源码或可执行文件。

- 新增实际识鸟页面分包检查：必须包含自动准备、实体屏幕录音及松手识别入口；缺失页面、混入旧分包或恢复“现在进入识鸟”的二次启动按钮均拒绝构建。
- 此检查同时接入 Vite 输出和 Xcode 的实际 App 校验；保留原有图片清单、哈希和硬件 CRC 回执链路，未改动录音/识别业务逻辑。
- 识鸟与导航针对性测试 106 通过；全量测试 2677 通过、16 跳过；TypeScript 和 13 项构建来源/图标回归通过。独立 GitHub 源码目录再次通过同一全量测试和 TypeScript。
- 13 张当前 OSS JPEG 已重新下载，长度和 SHA256 全部匹配原生清单。此检查不是 B 板或手机实测。
- 源码 SHA256：`67ec513adf51f5b03f91219b52396526b413995556771daeb5bcacc589342658`。
- Web SHA256：`99760b383270b88e1a04656308e3a1c9b31ae0f736daf245e7244277a3ecca39`。业务页面未改，因此与 23 号相同；本次确实重新执行了全部构建步骤。
- 产物目录：`/Volumes/PocketBuddy-iOS-Dev/Current/Releases/2026082824-WUU5nE`。14:49 上传完成，Apple 处理完成；用户此时报告实体收音链路失败，24 号包没有加入测试组，不能作为该故障的修复版。

使用顺序：连接 B 板 → 打开识鸟 Skill/说出识鸟指令 → 等圆屏录音提示 → 按住触屏约六秒后松手 → 等识别结果和鸟图。
首次黑屏识鸟需前台授权并主动开启；实体收音、手机中转及锁屏仍须用户用新版验收。24 号包准备阶段没有刷写硬件或安装/卸载手机 App；后续音频链路修复另见下文。未读取或联系其他会话。

## 2026082825 识鸟 10 秒音频链路修复

在用户报告真实硬件长按录音失败后，暂停分发 24 号包，先修复当前本地固件与 iOS 源码。
修复内容、可重复验证和仍需真人验收的边界见 [识鸟音频链路修复](BIRD-AUDIO-LINK-FIX-2026-08-28.md)。
已从正式工作区以新构建号 `2026082825` 完整归档；没有修改 24 号包冒充新版。

- B 板已更新为 `0.2.22-ojbadge-bird-audio`；只写应用分区，刷写前保留原应用备份，刷写后独立校验。
- 触屏鸟声录音最长 10 秒，松手可提前停止；满 10 秒自动结束，持续按住不会重复启动。完整 PCM 传到手机后，当前模型选取较强的 3 秒窗分析。
- 2026-08-28 15:20:24 CST，Xcode 确认上传成功；Apple 已处理完成。构建 ID 为 `2ce151c9-99da-4d3c-938c-a02ccfb140d0`，测试说明已保存。
- Apple 当前要求提供出口合规声明；该声明尚待用户确认，未代填、未加入测试组，不能宣称 TestFlight 已可下载。GitHub 推送按用户要求留到发布后。
- 已向当前连接的 iPhone 覆盖安装同一归档的开发签名版本，版本回读确认 `2026082825`；没有卸载或清除数据。这不是 TestFlight 下载证据。
- 归档和实际上传 IPA 均通过来源、画布、Frost Skills、识鸟及已确认小狗图标检查；主应用与 Widget 均为 25 号，共享权限及 18 张组件头像保留。
- 全量测试 2679 通过、16 跳过，TypeScript 与 13 项来源/图标回归通过；待推送的 GitHub 源码也独立通过测试和主应用、两个子应用构建。
- 本轮确认的是 10 秒标准音频回放、真实后端返回和 B 板图片解码回执。真人实体收音 → iPhone 的 BLE 上行及锁屏链路尚未验收，不以这些测试替代。

## 2026082826 健康咨询、统一回复音量与识鸟失败诊断整包

用户明确要求将最新识鸟失败修复与健康咨询一起打包。先前构建期间出现源码变化的候选已被 guard 拒绝，未安装/上传；本次从当前正式工程完整重建主应用、两个子应用与原生工程，使用新 DerivedData。

- 健康咨询改名与语音直达、Qwen＋本地文本检索的私有会话、教练同档回复音量见 [健康咨询发布记录](../development/HEALTH-CONSULTATION-RELEASE-2026-08-28.md)。
- 失败撤下旧鸟图、回到 Frost、分阶段错误与原始错误保留见 [识鸟失败诊断](BIRD-FAILURE-DIAGNOSTICS-2026-08-28.md)。保留 960000 字节缓冲及最长 10 秒录音。
- 2026-08-28 16:28 CST 归档通过，主应用与 Widget 均为 `2026082826`；来源、画布、Frost Skills、识鸟、小狗编译图标及 18 张 Widget 头像检查通过。
- 解包后的分发 IPA 再次通过全部上述检查，分发签名和共享 App Group 正确。IPA SHA-256：`681630945b768d62784c5d438d4f6cb7ff3447515c200b6ea15d10ed0b4b8694`。
- 源码 SHA-256：`d2a920f0bd66a060f468ccd611da3aa41f332def9af58b28b21e6c999af0fa3c`；Web SHA-256：`58339fd73bbec877218711fa3a295b1701de9d62abe788fab2a54705230d17f9`。
- 全量测试 2,696 通过、16 跳过；原生协议/生命周期、完整 10 秒 1,600 分片、0/8/16/32 KB/s 缓存排出回归通过。独立 GitHub 源码目录同样通过全量测试和类型检查。
- 16:29 CST 覆盖安装至连接的 iPhone，回读 `2026082826` 并成功启动，未卸载或清除用户数据。实板已运行 `0.2.23-reply-volume`；下行短音频实际排空 4,800 samples、零丢弃、codec volume 100，之后恢复普通音量。
- 归档与 IPA 位于 `/Volumes/PocketBuddy-iOS-Dev/Current/Releases/2026082826-vxLfFg`。上传和 Apple 可测试状态需独立确认，不把本机安装或上传成功视为 TestFlight 已可下载。
- 16:34:25 CST，Xcode 确认 `Upload succeeded`，Apple 已接受新包并开始处理。尚未代填出口合规声明或把待处理版本宣称为可下载。

真人实体录音 → iPhone → 服务 → 硬件回图/语音、实际听感和黑屏使用仍待验收。健康咨询当前要求 App 前台；原始语音本机识别，本次咨询文本经用户同意交给云端处理，不自动写入健康记忆。
