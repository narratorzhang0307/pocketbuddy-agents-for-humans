# 吧唧面板精简

- 默认界面仅保留连接、两句地图口令、必要运行状态和简短权限/费用提醒。
- 勾选项、手动转文字、语音生成、设备诊断在默认折叠的“更多”里；长“使用说明”已从组件源码删除，不再折叠隐藏。自动发送沿用默认开启；尊重用户以前保存的关闭选择，不强制覆盖。
- 已连接时仍可直接停止播放、调音量；错误和无声/不完整录音提示不隐藏。
- 只改 `FrostBadgePanel.tsx`、`FrostBadgeSpeechControls.tsx`、`FrostVoiceControls.tsx` 的展示，不改语音、BLE、观鸟或真实 GPS 种树逻辑。

## 验证

- 初次精简：7 个相关测试文件共 208 项通过，包含 7 项面板渲染检查；TypeScript 检查通过。
- 删除长文案后：6 个相关测试文件共 202 项通过，包含 8 项面板渲染检查。新增检查要求旧长段落在完整渲染结果中不存在，且默认状态的每段说明不超过 40 字；不是只检查折叠前的界面。
- 浏览器实际检查 430px 宽应用布局：默认面板无勾选框、无手动表单；展开“更多”后可见默认已开启的自动发送，再次收起正常。网页版蓝牙不可用提示不代表手机蓝牙测试结果。
- 完整 iOS 页面资源构建和资源检查通过。

## 长文案代码已删除（2026-08-28 13:12）

- 仅处理本对话截图中的文字：从 `FrostBadgePanel.tsx` 删除整块“使用说明”及六段长文；缩短手动识别、手动语音的解释，并移除重复展示的朗读全文。不是把这些长段落继续藏在折叠区。
- 保留简短连接状态、两句口令、按键提示和必要的权限/费用提示；不修改功能处理逻辑或默认偏好。
- 当前根目录的代码直接构建到独立输出，未改共享 `dist-ios`。新面板 `FrostBadgePanel-CLP4W5Vr.js` 的 SHA-256 为 `4cf2b7ea18580037101b07d17405480345c2dbea992b0a2fd9d6fba6a26bb961`，包内检查确认旧长段落不存在。
- 删除长文案后的安装包已成功覆盖安装到同一台 iPhone；证据目录：`/Volumes/PocketBuddy-iOS-Dev/Artifacts/badge-copy-only-20260828.c7WGIP/`。未卸载或清数据。

## 前一次手机安装（2026-08-28 13:06）

- 用户最新截图仍为旧面板；设备查询确认为 `0.1.0 (2026082810)`。同版本签名归档中有两份旧 `FrostBadgePanel` chunk，均含旧长文案、不含精简提示。这是旧页面资源仍被打进安装包，不是需要清除用户数据。
- 本次以已安装的 `2026082810` 完整原生归档为基底，保留观鸟、健康、桌面组件和 App Groups；在独立目录直接重新构建当前源码，不从会被覆盖的共享 `dist-ios` 复制构建结果。
- 新安装包版本 `2026082811`（App 和桌面扩展版本一致）。重新打包前，原生可执行文件、桌面组件可执行文件、BirdCatalog 和 Capacitor 配置的 SHA-256 与基底一致；沿用原签名权限和包含这台 iPhone 的现有配置描述文件，没有新增权限、删功能或改苹果账号。
- 安装包只有一份精简面板 `FrostBadgePanel-s0jPjJxH.js`，SHA-256 为 `13096ac740ffb84e657df8437ce52d44ce1d23a5aa9abcf64f62c987ef2ddb3d`；构建前后 3 个界面源码的哈希一致。训练结果桥、Her Motion 子应用与入口资源检查通过；208 项相关测试重新通过；完整签名验证通过。
- 13:05—13:06 `devicectl` 覆盖安装成功，随后从手机读回 `2026082811`，启动成功；20 秒后仍有对应新安装路径下的 App 进程（PID 7275）。没有卸载 App、清缓存或删除用户数据。
- 这是直接安装到已连接 iPhone 的开发签名包，**未上传新的 TestFlight 版本**。安装与启动已验证；不能将浏览器预览或进程检查说成已拍到手机 UI 截图。
- 独立产物和证据：`/Volumes/PocketBuddy-iOS-Dev/Artifacts/compact-badge-install-20260828.mXBtfp/`，包括 `build.mjs`、`build-verification.json`、`tests.log`、`App.app`、`device-install.json`、`installed-version.json`、`device-launch.json` 和 `running-app.json`。旧页面副本保存在包外 `previous-web-not-installed`，未装入手机。此前不完整包仍明确标记为 `Incomplete-NotForInstall.app`，从未安装。

## 此前尝试记录（12:07—12:29，已由上面的成功安装解决）

当时工程含桌面组件及 App Groups 配置，完整原生构建因主 App 的配置描述文件不含 App Groups、桌面组件缺少配置描述文件而失败。本次没有修改这些源码、功能开关或苹果账号权限。

尝试复用本对话先前已签名、安装并启动的 `0.1.0 (2026082806)` 原生程序生成界面增量包。原生签名验证通过；旧原生不含桌面组件，页面已有插件可用性检查。共享页面资源复制期间出现文件缺失，已停止打包，没有签名或安装这个不完整副本；副本改名为 `Incomplete-NotForInstall.app`。

本次独立产物目录：`/Volumes/PocketBuddy-iOS-Dev/Artifacts/compact-badge-20260828.T0VX6T/`。

2026-08-28 12:10 查询发现手机版本已变为 `0.1.0 (2026082807)`，因此没有覆盖这个较新的安装包。该版本并非本轮安装操作的结果，尚不能仅凭版本号断言包含精简 UI。12:12 设备返回 `passcodeRequired: true`，启动核对超时；需要用户解锁 iPhone 后继续核对实际安装资源/界面。`current-installed.json`、`current-launch.json`、`current-process.json` 保存本轮设备回执。

12:26 用户解锁后确认 `passcodeRequired: false`，手机版本仍为 `2026082807`。首次启动命令返回成功，但未能通过调试器读到安装包中的面板资源；调试器已退出。12:28—12:29 两次启动核对均被 iOS 返回 `Busy / Application failed preflight checks`，且未发现 App 运行进程。未卸载、覆盖手机应用或清理数据。需要用户在手机手动打开 App 并提供面板截图，确认实际精简效果；不能把启动命令成功或版本号当作 UI 验收。
