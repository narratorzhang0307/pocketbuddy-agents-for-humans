# 0828最终版

2026-08-28（Asia/Shanghai），用户明确要求将当前完整产品源码作为重要节点备份，并反馈：

> 目前为真机实测，都很完美。

这是用户提供的验收反馈。本次操作只做源码核对、GitHub 备份和回退节点保存，未重新安装手机、刷写硬件、部署服务器或独立复测所有功能。

## 固定节点

- 正式仓库：[narratorzhang0307/pocketbuddy](https://github.com/narratorzhang0307/pocketbuddy)。
- 发布分支：`main`；本地对应远端名为 `pocketbuddy`，不是 `origin`。
- 本节点提交备注：`0828最终版`。
- 带说明的版本标签：[`v2026.08.28-final`](https://github.com/narratorzhang0307/pocketbuddy/tree/v2026.08.28-final)。后续迭代使用新提交和新标签，不移动或覆盖这个标签。
- 对应 TestFlight：`0.1.0 (2026082828)`，2026-08-28 17:37 已确认处于内部测试分发状态。
- 产品源码基线：`fb7e249a354f8effa312463bca045291584e9404`；本次备份前的远端节点为 `060d0575c52d3ae041f6311c05863b31b69e5c56`。

Git 标签指向本次节点提交，提交的 Git tree 保存完整文件版本，不只是增量补丁。之后 `main` 继续更新，也能用标签取回这一版。

## 备份范围与核对

以正式工作区的当前文件内容逐项对照 GitHub `main`，而不是依据旧本地分支的状态或从历史 App 拷贝文件。本次核对确认产品代码已与远端一致，新增节点说明与 README 入口，不改变已验收的功能实现。

保存的内容包括：

- 主应用 `src/`、必要公开素材 `public/`、依赖锁文件和构建脚本。
- Frost Agent、Skill 与健康记忆实现，Photos 示例，已确认的线稿技能画布。
- iOS / Android 工程、原生蓝牙桥接、小狗图标、桌面组件与灵动岛。
- 高德路线规划、原生导航、健康咨询与语音回复、识鸟 10 秒录音及失败诊断代码。
- `server/`、服务端入口与部署说明。
- `lianlema-portable/` 和 `vendor/her-motion/` 两个训练子应用的源码、各自锁文件及必需离线教练音频。
- `hardware/ojbadge-agent-link/` 硬件固件源码与默认构建配置；当前 `PROJECT_VER` 为 `0.2.23-reply-volume`。
- 正式仓库已有的有效产品文档；没有因本地缺少文档而删除远端文档。

以下不属于公开产品源码备份，继续按现有忽略规则排除：密钥、`.env.local`、Apple 签名材料、设备与用户数据、私人照片/录音、验收原始媒体、依赖目录、训练权重、构建缓存、旧 App/IPA 和生成的 Web 分包。需要恢复运行环境时，私有配置与外部服务须另行提供。

**CI 配置例外：**本地 `.github/workflows/ci.yml` 仍由 `.git/info/exclude` 排除；现有规则记录的原因是 GitHub 凭据缺少 `workflow` 权限。本次未绕过忽略规则或变更授权，也未上传/启用此工作流。它不属于 App 或固件构建输入，源码仍可按下面的命令手动构建。

## 对应安装包与已有验证

完整发布过程见 [iOS 28 发布记录](IOS-ROUND-ACTIONS-RELEASE-2026-08-28.md)。以下是该次发布已记录的结果，不是本次重新跑出的测试结果：

- 全量测试：2,732 通过、16 跳过；224 个测试文件通过、3 个跳过。
- 原生识鸟生命周期、路线回放及鸟图素材检查通过。
- 实际 App 通过来源凭据、线稿画布、Frost Skills、识鸟、小狗编译图标和签名检查。
- 主 App 与桌面组件构建号、App Group 及离线头像资源一致。

| 对象 | SHA-256 |
| --- | --- |
| 发布来源 Source | `882fb1a5c97292d93a16c083a785271bb17cc77bac4dff98b91aea3c577de4ca` |
| 发布资源 Assets | `a0e04d62e3b9225d68ecb5d07b5a877e2b19476185e3388e45e5ebafe72d271e` |
| 上传 IPA | `2ad2898418cd09042fb7ec48707ef797314fc2fe3f1272956053e75692bf70dd` |

本节点是源码备份，不把 GitHub 提交等同于服务端部署，也不自动备份手机中的健康记录或云端数据。

## 以后如何安全回到这一版

推荐在一个尚不存在的新目录中取回标签，保留正在开发的工作区不动：

```sh
git clone --branch v2026.08.28-final --single-branch \
  https://github.com/narratorzhang0307/pocketbuddy.git pocketbuddy-0828-final
cd pocketbuddy-0828-final
git show --no-patch v2026.08.28-final
git switch -c codex/restore-0828-final
```

如果 `pocketbuddy-0828-final` 已存在，换一个新目录名，不要删除或覆盖已有目录。创建恢复分支后可以正常修改和提交；无需 `reset --hard`，也不强推覆盖共享 `main`。

随后按 [从当前源码完整构建](../development/CURRENT-SOURCE-BUILD.md) 安装主应用与两个子应用各自的锁定依赖、恢复私有配置并重新构建。不要拷贝旧 `dist-ios` 或旧 `.app` 代替重建。

- **恢复 iOS 安装版：**需要 macOS、Xcode、有效签名；归档使用 Apple 后台核对过的**新构建号**，不能再次上传 `2026082828`。仍须通过全部实际包检查。
- **恢复硬件：**使用标签中的固件工程和对应工具链重新构建；确认目标设备与分区后再刷写。检出 Git 标签不会自动刷机或恢复设备数据。
- **恢复服务器：**按标签中的部署说明部署；密钥、模型服务、数据库和远端配置单独管理。检出标签不会自动回退生产服务器或用户数据。

后续如需撤销已共享的代码变更，应先核对差异，再用新的回退提交处理；不改写这个稳定节点或已经共享的历史。
