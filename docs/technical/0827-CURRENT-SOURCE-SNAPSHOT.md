# 0827 当前源码完整备份（记忆闭环改造前）

快照备注：按用户要求保留 `0827` 标记；跨午夜后于 2026-08-28（Asia/Shanghai）完成备份。目标仓库：`narratorzhang0307/pocketbuddy`，备份分支：`codex/0827-full-source`。

## 目的与范围

在开始新一轮「今日记忆 + 长期健康档案 + Qwen 决策 + MiniMax 播报 + Skill 结果回写」改造之前，保存当前工作区可读取的完整产品源码。此提交是回退基线，不代表上述闭环已经完成。

包含主应用、Frost Agent/Taskmaster、服务端和部署脚本、医院 Agent、练了吗源码、Her Motion 源码及当前内置页面、iOS/Android 与原生蓝牙插件、`hardware/ojbadge-agent-link` 硬件固件、测试、技术文档及未被忽略的产品运行资源。保留当前已有的跨任务改动，包括女性运动直达相机修复；不重置、不清理用户工作区。

该分支以目标仓库已有的 `0827` 快照为父提交，单独记录当前文件树，不覆盖 `main`，不引入其他远端尚未发布到本仓库的历史。提交时使用独立 Git 索引，不切换正在使用的工作分支，不修改原暂存区。

## 明确不上传的内容

- 真实 API 密钥、环境变量、签名证书、个人健康/患者数据、本地数据库及录音。
- `node_modules`、虚拟环境、缓存、安装包、临时构建产物、训练数据与大模型权重；运行环境仍需按现有文档安装和配置。
- 私人演示/参赛材料、原始诊断媒体，以及现有 `.gitignore` / `.git/info/exclude` 排除的文件；包括当前凭据权限未覆盖的工作流文件，不强行上传。
- 未挂载外接 SSD 中的训练/归档目录：无法声称已备份本机当前不可读取的最新文件。目标仓库上一版已发布的 `training/` 源码原样保留在此快照中，并不代表重新校验了 SSD 最新版本；外接盘内容不作改动。

备份前对候选文件进行大小、忽略规则和常见密钥模式检查。唯一匹配项为 MiniMax 单元测试中的假凭据；不是生产密钥。医院 Agent 的运行记忆目录 `data/memory_data/` 也不上传。没有为了生成备份调用付费模型或上传用户健康数据。差异检查发现既有医院 Agent/第三方资源中的行尾空格和 CRLF 提示，作为原样备份不执行全库格式重写。

## 当前验证与设备边界

女性运动直达相机修复已通过 202 项相关测试、主应用及子应用类型检查、Web 构建和模拟相机页面验收，详情见 `HER-MOTION-DIRECT-CAMERA-2026-08-27.md`。此次备份不声称对所有现有功能重新完成验收。

当前外接开发盘未挂载，完整 Xcode 与 iOS 构建资源链接不可用；尚未把本次源码安装到 iPhone。已验证的隔离 Web 构建目录因磁盘空间紧张被清理，可从源码重新构建；修改前源码和子应用备份仍保留。现有手机、硬件和线上服务不因本次 GitHub 备份而自动更新。

## 下一阶段（尚待实施）

按本地日期汇总真实、可追溯的饮食/运动/步数事实，隔离演示数据；把长期档案与今日状态作为用户授权的最小上下文交给 Qwen；校验结构化建议，再经 MiniMax 与硬件反馈。Skill 完成结果回写事实，刷新今日状态。保留现有硬件语音入口与直达相机行为，并分别验证离线、权限拒绝、重复回调、过期建议和锁屏限制。
