# 0827 二次清理：树莓派与剩余缓存

按后续要求继续清理旧树莓派内容。没有修改当前 UI 行为、模型配置、前后端接口或现有工具链；独立任务 `hardware/ojbadge-agent-link` 完全排除，不删除、不迁移、不改配置、不停止进程，也不把其未完成内容纳入本轮 GitHub 提交。

## 已移出

- `树莓派硬件/`、`hardware/frost-edge-google/`：旧 Raspberry Pi / Whisplay 实现及制作材料。
- `_local-backups/`：旧 Pi、Sunset Radio 等重复备份。
- `docs/assets/hardware/` 中的旧图集与渲染源文件，保留一份归档说明。
- `public/hardware-digital-twin.html` 及 `dist/` 中的副本：未被当前应用引用的旧硬件独立演示页。
- 两个树莓派专用的文档更新/图集渲染脚本。
- `var/toolchains/pocket-mnn-android-build/`、旧 Gradle 构建缓存与 Python 缓存；现有 JDK、Android SDK、原生库及构建脚本未删除。
- PPT 重构目录中明确命名的渲染缓存和 QA / 4K 预览目录；PPT 正稿、正式图片、文档和视频保留。

这 24 项目标原占用约 426.9 MiB。逐文件验证 SSD 归档后才移除；数据迁移与说明文件占用很小，实际净释放约 427 MiB。其他并行任务仍在生成文件，因此整个项目或磁盘的实时大小可能同时增长。

## 唯一运行源码调整：数据位置

`EarthAnswerAgentPage.tsx` 原先从树莓派目录导入 365 天答案，不能直接连同树莓派工程删除。

已把同一份 JSON 原样复制到 `src/app/data/earthAnswers365.json`，组件仅修改一行导入路径，页面逻辑、文案、样式及存储键不变。数据为 157,447 字节、365 条唯一日期，SHA256：

`136c26a010ab3ca43f036880305d6b2166bc898f3c9efe098740dec6c807db72`

## 备份与边界

备份位于 Extreme SSD 的 `谷歌大赛/二次清理_树莓派_2026-08-27/树莓派与剩余缓存_完整可恢复_2026-08-27.tar`。共核对 1,414 个文件、306 个目录，归档 SHA256：

`304bab6b85c09a1623ecb973c528dc338ab80767c62a3c10b4f0c1545b0abe78`

完整目标、保护清单和逐文件结果保存在同目录；需要恢复时解压到新建的独立目录，按需复制，不要覆盖当前项目。

`training/` 本轮开始时未挂载；其内容仍在 SSD 的训练工作区镜像中，没有将 Git 显示的缺失误作本轮删除，也没有自动挂载或改动该卷。

仍保留的主要占用是运行依赖、本地模型、运动功能环境、Android SDK/JDK、应用资源、Git 历史和正式材料；不能为了缩小数字把它们当作无用缓存删除。

## 验证

- 清理前后均为 160 个测试文件、1,856 项测试通过；类型检查与生产构建通过。
- 130,071 个保护范围内的文件，仅有计划内的组件导入路径改变，并新增原样数据文件；没有其他内容变化。并行硬件任务目录不参与扫描或改动。
- 清理后构建的 873 个保留文件与清理前对应文件逐字节一致，仅移除旧 `hardware-digital-twin.html` 独立演示页。
- 真实模型、树莓派、Android 真机和另一硬件任务均未启动或联调；构建仍保留原有的大分包提示。
