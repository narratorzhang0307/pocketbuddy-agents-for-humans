# 代码与提交材料一致性审查

审查日期：2026-07-14

## 结论

架构图与红色修订口播中的核心技术陈述均已落实到当前代码；GMI 已从主链降为可选备用传输。提交材料没有把 Google Search grounding、Vertex AI、Firebase、ADK 或未实现的端侧向量检索写成现有能力。

## 声明—实现—验证

| 提交声明 | 代码证据 | 自动验证 | 状态 |
|---|---|---|---|
| 官方 Gemini API 直连优先 | `server.mjs`、`vite.config.ts`、`frost-agent/provider-compat/googleGemini.ts` | `provider.test.ts` | 已实现 |
| GMI 仅作 Google-only 备用 | `frost-agent/provider-compat/gmi.ts`、云端 provider 选择逻辑 | `provider.test.ts`、`taskModels.test.ts` | 已实现 |
| Flash-Lite / Flash / Pro 按任务分工 | `frost-agent/harness/taskModels.ts` | `taskModels.test.ts` | 已实现 |
| Gemini 明信片图片生成官方直连优先 | `src/app/lib/skills/geminiImage.ts`、`/api/gemini-image` | 客户端请求边界测试、生产构建 | 已实现；GMI 仅为无官方 key 时备用 |
| Gemma + MediaPipe 浏览器端推理 | `frost-agent/edge/gemmaEdge.ts`、`contract.ts`、`OnDeviceBrainPanel.tsx` | `gemmaEdge.test.ts`、生产构建、浏览器真实加载与生成 | 已实现；官方 Gemma 3n E2B Web 权重已安装 |
| Router 规则 → Gemma → Gemini | `frost-agent/harness/router.ts` | `router.privacy.test.ts` 与既有 router 测试 | 已实现 |
| 隐私图片不静默上云 | `visionRead.ts`、`cloudUploadBoundary.ts`、`ExhibitionRunPage.tsx` | `cloudUploadBoundary.test.ts` | 已实现 |
| 云端图片二次确认 | `ExhibitionRunPage.tsx` 的待确认文件状态与确认弹窗 | 类型检查、生产构建 | 已实现 |
| Gemini 同次生成中英导览、时间线、文化桥 | `src/app/lib/exhibition/curator.ts` | `curator.cross-cultural.test.ts` | 已实现 |
| Agent Forge 不冒充实时搜索 | `src/app/lib/agent/research.ts`、`AgentForgePage.tsx` | `research.test.ts` | 已实现；所有候选默认 `pending` |
| 所有地图写入需确认 | 各领域 `pin.ts` 与 UI 确认入口 | 既有 pin / contract 测试 | 已实现 |
| RunTrace 标出执行阶段与降级 | `src/app/lib/observe/bus.ts`、`RunTrace.tsx` | `bus.test.ts` | 已实现 |
| MediaPipe 可迁移边界 | `EdgeModel` 契约隔离业务层；未保留空的 `embed()` 伪接口 | 类型检查、Gemma 测试 | 已实现 |

## 本轮验证结果

- `npm run typecheck`：通过；
- `npm test -- --run`：52 个测试文件、1336 项测试全部通过；
- `npm run build`：通过；
- `node --check server.mjs`：通过；
- 十张 技术 架构与审核图：2160×2880，PNG / SVG 重新渲染并逐张检查；
- 字幕 Word：由最终 SRT 重新生成，红色新增/替换与红色删除线分开标记，并逐页渲染检查。
- Gemma 模型：`3038117888` 字节，SHA-256 `b6c8e1081ec80730f14473a5ece941b48da5d8e2a80c97c2963da153f3eff3d2`；开发与生产路由的 `HEAD`、`GET Range` 均实测通过；
- 端侧运行：浏览器内成功加载为“运行中”，实测文本生成返回“端侧 Gemma 已连接”，耗时 918ms。

## 演示前仍需外部准备

1. 配置自己的 `GEMINI_API_KEY`，审核现场优先展示接口返回的 `provider=Google Gemini API` 与 `transport=google-gemini-api`；
2. 当前开发目录已安装并校验 Gemma 3n E2B MediaPipe Web 权重；部署新域名时按 `.local-models/README.md` 将权重目录与 `server.mjs` 一起上传，静态托管平台则需将 `VITE_GEMMA_MODEL_URL` 指向支持 Range/CORS 的模型地址；
3. 正式演示前做一次真实网络调用和目标设备 WebGPU 冒烟测试。仓库不会提交任何 API key，也不会把超大模型权重伪装成代码内置资产。

## 旧字段兼容说明

展品本地存储中仍保留 `gmiConfidence`、`gmiContributions`、`gmi-vision` 等旧版字段/枚举，以便读取旧浏览器缓存和回归 GMI 备用路径；它们不是当前默认 provider。新云视觉记录写入 `gemini-vision`，界面统一显示 Google Gemini，provider 的真实传输归属以服务端返回的 `transport` 为准。
