{
  "name": "pocketbuddy-api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.6.2",
    "firebase-admin": "^13.5.0",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "tsx": "^4.20.5",
    "typescript": "^5.6.3",
    "vitest": "^4.1.9"
  }
}
